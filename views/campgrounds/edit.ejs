<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit <%= campground.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <nav class="navbar navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="/campgrounds">YelpCamp</a>
      <a class="btn btn-outline-light btn-sm" href="/campgrounds/<%= campground._id %>">Cancel</a>
    </div>
  </nav>

  <div class="container">
    <h1>Edit Campground</h1>

    <!-- ✅ Add enctype for file upload -->
    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="card p-4 shadow-sm">

      <div class="mb-3">
        <label class="form-label">Title</label>
        <input type="text" name="campground[title]" value="<%= campground.title %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Location</label>
        <input type="text" name="campground[location]" value="<%= campground.location %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Price ($)</label>
        <input type="number" name="campground[price]" value="<%= campground.price %>" step="0.01" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="campground[description]" rows="4" class="form-control"><%= campground.description %></textarea>
      </div>

      <% if (campground.image) { %>
        <div class="mb-3">
          <label class="form-label">Current Image</label><br>
          <img src="<%= campground.image %>" class="img-fluid mb-2 rounded" style="max-height: 300px;">
        </div>
      <% } %>

      <div class="mb-3">
        <label class="form-label">Upload New Image</label>
        <input type="file" name="image" class="form-control">
      </div>

      <button type="submit" class="btn btn-primary">Update Campground</button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
